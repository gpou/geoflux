%tr
  %td= port.code
  %td= port.name
  %td= port.country.name
  %td.text-center
    .btn-group.btn-group-xs
      %a.tablectrl_small.btn.btn-default.tipS{:href => edit_admin_port_path(port), :title => t("helpers.edit_model", model: t("activerecord.models.port", count: 1))}
        %span.glyphicon.glyphicon-pencil
      = link_to admin_port_path(port), :method => :delete, data: { confirm: t("helpers.confirm_delete_model", model: t("activerecord.models.port", count: 1)) }, :class => "tablectrl_small btn btn-default tipS", :title => t("helpers.delete_model", model: t("activerecord.models.port", count: 1)) do
        %span.glyphicon.glyphicon-remove
