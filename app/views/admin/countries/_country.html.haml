%tr
  %td= country.code
  %td= country.name
  %td.text-center
    .btn-group.btn-group-xs
      %a.tablectrl_small.btn.btn-default.tipS{:href => edit_admin_country_path(country), :title => t("helpers.edit_model", model: t("activerecord.models.country", count: 1))}
        %span.glyphicon.glyphicon-pencil
      = link_to admin_country_path(country), :method => :delete, data: { confirm: t("helpers.confirm_delete_model", model: t("activerecord.models.country", count: 1)) }, :class => "tablectrl_small btn btn-default tipS", :title => t("helpers.delete_model", model: t("activerecord.models.country", count: 1)) do
        %span.glyphicon.glyphicon-remove
